// // write a function which takes a list of numbers as input and returns its sum:
// // ps: User spread operator to take input(...)

// function sum(...numbers) {
//   let total = 0;
//   for (let i = 0; i < numbers.length; i++) {
//     total = total + numbers[i];
//   }
//   return total;
// }
// var result = sum(1, 2, 3, 4, 5);
// console.log(result);

// // console.log(sum(1, 2, 3, 4, 5));

var arr = [1, 2, 3];
var js = arr.reverse();
console.log(js);
